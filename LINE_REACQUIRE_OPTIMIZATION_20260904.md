# 丢线重捕优化记录（2026-09-04）

## 现象与原因

旧逻辑在丢线原地搜索时，只要 X1～X4 任意一路检测到黑线就立即停止
`EncoderTurn`。最外侧 X2/X4 通常最先碰到黑线，随后状态机进入短暂的
向前差速捕线；黑线尚未进入中间探头就再次丢失，于是形成“原地转一点、
向前一点、再次丢线”的蠕动循环。

同时搜索目标只有 1800 counts/s，接近四轮连续控制低速区，负载和地面
横向摩擦会让实际原地转向显得更慢。

## 本次修改

- 丢线原地搜索目标提高到 3600 counts/s，仍由四轮编码器闭环控制。
- X2/X4 外侧探头只作为预告，不再停止原地搜索。
- X1/X3 中间探头必须连续检测到黑线 12 ms，才请求结束搜索。
- 结束搜索使用 `EncoderTurn_RequestStop()`，由 DriveBase 执行已有的非阻塞
  换向保护、短制动和编码器稳定过程；在此期间上层速度命令保持释放。
- 若制动后或 250 ms 低速捕线期间中间探头再次丢线，保持原预测方向重新
  搜索，不先执行向前点动。
- 仍保留编码器位置控制的单次 360° 搜索上限；到达上限或编码器故障停车。

## 编译与烧录

已使用 `build_unified_motion.ps1` 完成全量编译和链接：

- text：62516 bytes；data：64 bytes；bss：3512 bytes。
- HEX SHA-256：`AE6084743882B1D1015C143936E03AB2808F1A0AA1132BBEFBB155F897779DE9`。
- BIN：62584 bytes；SHA-256：
  `F398871D27E49D1BB95606E2D79B4C4CD625E3073C87265962636E9F772C8EB9`。

正式固件使用 COM11、57600 和 `-PreserveLastPage` 完成选择性擦除、写入、
逐字节读回及 GO：31 个固件页、62584 bytes，全部成功。启动后确认
`DRV M=0 P=0 F=0`，四轮目标、实测和 PWM 均为 0。

## 悬空测试

普通 KEY2 运行 3 秒后自动发送 `0`：

- 四轮目标均为 5273 counts/s。
- 四轮实测约 5300～5400 counts/s，PWM 约 2870～2891。
- 四路编码器非法跳变均为 0，DriveBase 故障为 0；停车后四轮归零。

为了不依赖人工移动黑线，`-LineTrackingLiftTest` 构建会在进入正式主循环前
传入合成掩码序列：X1+X3中央线 → X1方向提示 → 全白丢线 → 仅X2外侧线
→ X1中央重捕 → X1+X3居中。该钩子只在定义
`LINE_TRACKING_LIFT_TEST` 时存在，正式固件不包含自动测试动作。

诊断固件悬空结果：

```text
LTST OUTER search=150 valid=0 wrong=0
LTST CAPTURE valid=148 final-forward=175
LTST OUTER BEGIN M1=+667 M2=+627 M3=+2939 M4=+3006
LTST OUTER END   M1=-543 M2=-586 M3=+4087 M4=+4163
LTST RESULT PASS MASK=0 DRIVE_F=0
```

这证明仅 X2 外侧见线的整个阶段仍由原地搜索占有电机，没有产生向前速度
命令；左侧两轮编码器减少、右侧两轮增加，方向符合预测左转。X1 中央见线
后完成制动并交回低速捕线，四轮编码器无故障。

测试后已重新烧回上述正式 BIN，再次逐字节读回成功；启动输出中不存在
`LTST START`，并再次确认默认停车和四轮零输出。电池测试期间约 8.22 V。

## 验证边界

电脑端编译、正式固件烧录/读回和合成输入悬空测试已完成；真实黑线宽度、
车轮落地横向摩擦、搜索制动后的车身惯性以及弯道重捕仍必须进行地面测试。
悬空编码器结果不能替代地面循迹效果。

## 后续修改：缩短重捕交接停顿

原状态机在中央探头连续确认 12 ms 后依次等待：

- DriveBase 30 ms 换向保护；
- 22 ms 有界反向转矩制动；
- 位置控制器 90 ms 通用静止观察；
- line tracking 额外 70 ms 前进保护。

因此确认黑线后约有 212 ms 的停车交接。现在保留前两项安全制动；一旦遥测
确认制动脉冲已经结束且位置状态仅剩 `DRIVE_POSITION_SETTLING`，立即取消该
次位置动作，并在下一控制周期进入原有 250 ms 低速捕线。预计停车交接约为
52 ms 加一个主循环周期。正常循迹转弯、全局制动参数和其他位置动作均未改。

诊断程序同时新增 `first-ms` 统计；中央探头出现后首次得到双侧正向捕线命令
必须不晚于 120 ms，否则设置失败位 `0x80`。这是可执行的悬空回归判据，不是
本次已经取得的实物结果。

本次先完成电脑端全量编译和链接：

- 正式版：text 63028、data 64、bss 3512，BIN 63096 bytes；
  HEX SHA-256 `E3BC906E8CCB81A9B6EF360399B5E3DB2C0BFD9DB2DF1728822842C0209B3579`，
  BIN SHA-256 `8264A8F75833056364B1FD9B48D0553C621604B7D567C41F61703CE5F5FB09EB`。
- 悬空诊断版：text 64728、data 64、bss 3512，BIN 64796 bytes；
  HEX SHA-256 `ABBE6C69B74FC4D2B4153BCBCBBB38D4C453015009634BFD19F77C58185374BC`，
  BIN SHA-256 `994E36271EA3EA5EAF6D587945085A00F942540604330C9C254452E838BC803A`。

用户随后明确要求烧录。正式 BIN 已通过 COM11、57600 baud 和
`-PreserveLastPage` 写入：ROM bootloader 应答成功，选择性擦除 31 个固件页
且保留最后一页标定数据，写入 63096 bytes，逐字节读回验证 63096 bytes，
最后 `GO 0x08000000` 成功。正式 BIN 中静态确认不含 `LTST START`。

本次复位后的 USART1 被动读取没有收到遥测文本，因此只能确认电脑端编译、
bootloader 烧录/读回及启动命令成功，不能声称已由运行时遥测再次确认默认
停车。新的悬空诊断固件没有烧录，未进行新一轮悬空或地面测试；地面测试由
用户自行完成。

## 后续修改：后轴支点扫线

落地反馈表明，四轮同速反向的中心原地转向受轮胎摩擦和载荷影响，实际旋转
中心偏向车头，表现为“车头接近不动、车尾摆动”。丢线重捕不需要改变整车
朝向动作的通用几何，它需要让位于车头的四路探头扫过附近黑线。

因此新增 `EncoderTurn_StartRearPivot()`，仅用于丢线搜索：

- M1 左前轮和 M3 右前轮按原预测方向反向转动，并各自接受编码器位置闭环；
- M2 左后轮和 M4 右后轮目标及输出保持为零，让后轮接地点成为近似支点；
- 中央探头确认、52 ms 有界制动交接、250 ms 低速捕线和 360 度轮程安全上限
  均保留；
- 原 `EncoderTurn_Start()` 接口及八字、正方形、绕障转向均未修改。

诊断版同步把判据改为：左转搜索期间 M1 计数应减小、M3 计数应增加，M2/M4
相对搜索阶段起点的变化均不得超过 20 counts。正式固件静态确认不含
`LTST START`，`motorPWM.c` 相对统一运动基线没有变化。

该修改先完成电脑端编译和链接：

- 正式版：text 63140、data 64、bss 3512，BIN 63208 bytes；
  HEX SHA-256 `6E0BFDFFE8F687661DD7A505F0204DA0D9554105D67AA5B6F0FB9DAA31F9B9D3`，
  BIN SHA-256 `A04E9AA8491FD5639D5E67C3423C3969AF8E80E7E285D87FD904E20FBBE24079`。
- 悬空诊断版：text 64884、data 64、bss 3512，BIN 64952 bytes；
  HEX SHA-256 `8F03600282E4A0128F8C27FBE6707134ACCCC830B212B5B0CF19F7E89A12DDF5`，
  BIN SHA-256 `1F165CAA413A1E56E26035F98CDB016F849A4D3F229F97CB2FF2440299F317AA`。

用户随后明确要求烧录。正式 BIN 已通过 COM11、57600 baud 和
`-PreserveLastPage` 写入：ROM bootloader 应答成功，选择性擦除 31 个固件页
并保留最后一页标定数据，写入及逐字节读回验证 63208 bytes，最后
`GO 0x08000000` 成功。诊断版未烧录，也没有进行新的悬空或地面测试。

后轴支点只是四轮滑移底盘的受力偏置，不是几何约束；没有 IMU 时，编码器
只能证明前轮转过设定轮程，不能证明车尾在地面上绝对不动。

## 后续修改：抑制扫线时后退

两前轮等幅反转的理论平均纵向速度为零，但落地后的左右抓地力、负载和电机
响应并不相同，实测出现整车大幅后退。新的后轴支点接口增加内侧反转比例：

- 外侧前轮保持 3600 counts/s 向前；
- 内侧前轮仅以外侧的 45%，即约 1620 counts/s 反转；
- 两个后轮仍保持零目标和零输出；
- 左右搜索完全镜像，45% 仍高于当前连续速度控制的低速边界，避免退化为
  点动脉冲。

这样转向速度由两前轮速度差继续提供，但平均纵向目标明确偏向前方。诊断版
新增外侧前轮计数增量必须大于内侧反转计数增量的检查，同时继续要求 M2/M4
变化不超过 20 counts。

该修改先完成电脑端编译和链接：

- 正式版：text 63396、data 64、bss 3512，BIN 63464 bytes；
  HEX SHA-256 `1372B9C0864322D23589F8476B74B11A13020E670ADBA95D31DD96A874D7DABC`，
  BIN SHA-256 `A7A4E702F7E5F6EC707B0D82A0D74A3EDB85119EF610D1197FB0F73A12F2D6C6`。
- 悬空诊断版：text 65172、data 64、bss 3512，BIN 65240 bytes；
  HEX SHA-256 `734A5D95D2395ADDCE3D3696A11291E6A694B05242AFF221E4E74760E3380B1E`，
  BIN SHA-256 `E933713BD86A1AF02BDB6FD345DD24A10F7A4C578201411861E6704243ACE9E9`。

用户重新连接小车后，正式 BIN 已通过 COM11、57600 baud 和
`-PreserveLastPage` 写入：ROM bootloader 应答成功，选择性擦除 31 个固件页
并保留最后一页标定数据，写入及逐字节读回验证 63464 bytes，最后
`GO 0x08000000` 成功。诊断版未烧录，也没有进行新的悬空或地面测试。

新的前进偏置只能从轮速目标上证明不会主动要求整体后退，实际地面位移仍需
测试确认。
