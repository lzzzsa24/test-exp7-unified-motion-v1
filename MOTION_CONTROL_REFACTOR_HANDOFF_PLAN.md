# 四轮小车基础运动控制重构计划（供另一个 Codex 对话执行）

生成日期：2026-09-04  
工作区：`F:\myproject\jidian`  
当前基线工程：`F:\myproject\jidian\project\test-exp7-line-bypass-v2`

> 本文是执行计划，不代表其中修改已经实施。接手对话必须先重新读取当前文件并验证基线，不能把本文中的历史观察当成最新实车结论。

> 在 `test-exp7-unified-motion-v1` 副本中，本计划已进入软件实施状态；
> 实际代码、编译证据和仍待实车验证的项目见 `README_UNIFIED_MOTION.md`。
> 本文保留原貌作为需求来源。

## 1. 任务目标

在一个新的独立 STM32CubeIDE 工程中重构四轮小车的基础运动控制，使系统形成清晰的两层闭环：

```text
循迹、绕障、8字、正方形等上层规划
                  ↓
       四轮目标速度／目标位置
                  ↓
       四轮编码器反馈控制
                  ↓
             motorPWM.c
```

重点解决：

1. KEY1、KEY2 正常循迹目前只根据黑线传感器直接下发左右 PWM，没有四轮编码器速度闭环。
2. 当前低于 `1412 counts/s` 时不执行速度 PI，而使用固定 `3100/3300 PWM` 的脉冲密度驱动，容易出现抖动、短转向过冲和部分轮子单独运动。
3. 编码器直线／转向到达目标后，各轮会分别停下，可能出现只剩一个轮子继续转、车体扭动。
4. 电池电压只显示，不参与动力前馈补偿。
5. 转向启动冲击和反向制动直接写 PWM，绕过统一速度控制。
6. 编码器大量使用绝对增量，只能证明轮子产生脉冲，不能验证实际方向。
7. 普通停车为 `0/0` 滑行，紧急停车距离可能偏长。

最终目标不是宣称“所有动作都是 PI”，而是明确区分：

- 可连续旋转速度区：四轮编码器 PI 速度闭环。
- 低于电机稳定转速的小角度、短距离和圆弧内轮：编码器位置脉冲控制。
- 轨迹终点：四轮进度同步与统一停车。
- 黑线循迹：黑线传感器作为外环，编码器速度控制作为内环。

## 2. 必须先读的文件

开始修改前完整阅读：

1. `F:\myproject\jidian\car_coding_spec.md`
2. `F:\myproject\jidian\project\test-exp7-line-bypass-v2\README_LINE_BYPASS_V2.md`
3. `F:\myproject\jidian\project\test-exp7-line-bypass-v2\README_ENCODER_FIGURE8.md`
4. `F:\myproject\jidian\project\test-exp7-line-bypass-v2\ENCODER_SPEED_CONTROL_20260903.md`
5. `F:\myproject\jidian\project\test-exp7-line-bypass-v2\BUZZER_PHRASE_40077493715_INTEGRATION.md`
6. 下列源码：
   - `Core/Src/main.c`
   - `Core/Src/motorPWM.c`
   - `Core/Src/motion_advanced.c`
   - `Core/Src/wheel_encoder.c`
   - `Core/Src/wheel_speed_control.c`
   - `Core/Src/encoder_linear.c`
   - `Core/Src/encoder_straight.c`
   - `Core/Src/encoder_turn.c`
   - `Core/Src/line_tracking.c`
   - `Core/Src/line_obstacle_bypass.c`
   - `Core/Src/figure8_encoder.c`
   - `Core/Src/square_encoder.c`
   - `Core/Src/ultrasonic_avoid.c`
   - `Core/Src/ir_avoid.c`
   - `Core/Src/stm32f1xx_it.c`

## 3. 硬件与现有约束

- 控制板：YB-DSF01-V1.1。
- MCU：STM32F103ZETx。
- 电机 PWM：TIM1、TIM8，周期 `3599`，约 20 kHz。
- 超声波：PF11/TRIG、PF12/ECHO，TIM2 为 1 MHz 计时。
- 四路编码器当前均已修复：
  - M1：PD12/PD13
  - M2：PA15/PB3
  - M3：PA0/PA1
  - M4：PB4/PB5
- TIM6 以 20 kHz 软件采样四路 AB 相。
- PA15、PB3、PB4 依赖 `__HAL_AFIO_REMAP_SWJ_NOJTAG()`，必须保留 SWD。
- 轮胎直径约 47 mm，左右轮中心距约 129 mm，前后轴距约 129 mm。
- `motorPWM.c` 中已有实车确认的电机方向映射，不得随意交换方向。
- K210 当前取下，视觉功能保持禁用，但不得破坏现有 UART 接口。
- 蜂鸣器 PG12 已接入五音节非阻塞短句，并有安全优先级仲裁，重构时必须保留。
- KEY1=综合循迹绕障、KEY2=仅循迹、KEY3=一次八字、遥控/串口数字4=正方形、数字0=停车，不能静默改变。

## 4. 不可违反的工作规则

1. 不直接在基线工程上试验。先复制为新的独立工程，建议名称：
   `F:\myproject\jidian\project\test-exp7-unified-motion-v1`。
2. 同步修改 `.project`、`.cproject`、`.ioc` 和 `.settings` 中的工程名与路径，检查是否残留旧工程名。
3. 在修改前记录基线文件清单、文件数量、当前 HEX SHA-256 和关键源码 SHA-256。
4. 使用 `apply_patch` 小步修改，不整体覆盖已经验证的文件。
5. 不修改 `motorPWM.c` 中已经确认的四轮正反方向映射。
6. 不删除或削弱编码器断线、失速、动作超时、红外异常和超声波异常的安全停车。
7. 未经用户在本轮明确说“烧录”，只允许修改、编译、生成产物和哈希；不得自动烧录。
8. 明确区分：电脑端编译成功、烧录/读回成功、四轮悬空测试成功、有载荷地面测试成功。
9. 未连接实物或未获得用户反馈时，不得声称速度、距离、转角或轨迹已实车验证。

## 5. 当前控制结构的基线判断

接手对话应重新验证以下判断：

| 场景 | 当前实现 |
|---|---|
| KEY1 正常循迹 | 黑线传感器纠偏，但轮速为开环 PWM |
| KEY2 正常循迹 | 黑线传感器纠偏，但轮速为开环 PWM |
| KEY1 绕障直线段 | 编码器距离控制；较高 CPS 使用 PI，低速和末端使用固定高 PWM 脉冲 |
| KEY1 绕障 15° 转向 | 编码器决定目标计数；当前静态计算一轮约 312 计数，减速后容易直接进入低速脉冲区 |
| KEY2 丢线搜索 | 编码器 360° 转向；主体可进入 PI，末端仍转入脉冲区 |
| KEY3 八字 | 外轮可进入 PI；220 mm 半径下内轮目标约 309 CPS，使用脉冲区 |
| 数字4正方形直线 | 四轮编码器速度 PI + 距离停止 |
| 数字4正方形转向 | 目标 1200 CPS，低于当前 1412 CPS 分界，主要使用固定 PWM 脉冲 |

当前 `motorPWM.c` 的每轮标定属于开环前馈，不是编码器实时闭环。电池监测当前仅供 OLED 和 UART 显示。

## 6. 分阶段实施计划

### 阶段0：建立独立工程与可回滚基线

1. 复制完整基线工程到 `test-exp7-unified-motion-v1`。
2. 校正 CubeIDE 工程身份和构建路径。
3. 在不改源码的情况下先编译新副本。
4. 对比基线与副本的 ELF 尺寸、HEX 哈希和符号；若因路径或时间戳造成哈希不同，至少确认反汇编和链接段无意外变化。
5. 建立 `BACKUP_MANIFEST.md`，记录源工程、复制时间、基线 HEX 和哈希。

完成条件：新副本能够独立导入和编译，基线工程未改动。

### 阶段1：建立唯一底盘输出层

建议新增：

- `Core/Inc/drive_base.h`
- `Core/Src/drive_base.c`

建议接口：

```c
typedef enum {
  DRIVE_STOP_COAST = 0,
  DRIVE_STOP_BRAKE
} DriveStopMode;

void DriveBase_Init(void);
void DriveBase_Task(uint32_t now_ms);
void DriveBase_SetWheelCps(int32_t m1_cps,
                           int32_t m2_cps,
                           int32_t m3_cps,
                           int32_t m4_cps);
void DriveBase_SetSideCps(int32_t left_cps, int32_t right_cps);
void DriveBase_Stop(DriveStopMode mode);
void DriveBase_GetTelemetry(/* 四轮目标、实测、PWM、故障 */);
```

要求：

1. `motorPWM.c` 保持最低硬件层，只负责方向映射、每轮前馈和比较寄存器。
2. 除 `drive_base.c`／速度执行层外，不允许其他运行模块直接调用 `pwm_motor*()`。
3. `line_tracking.c`、`encoder_turn.c`、`encoder_linear.c`、`encoder_straight.c` 不再直接写 PWM。
4. 急停、模式切换、编码器故障、传感器故障均通过同一个 `DriveBase_Stop()`。
5. 先只做调用路径迁移，保持原有实际 PWM 数值不变，避免“架构和参数同时改变”。

静态验收：使用 `rg` 确认直接 `pwm_motor*()` 调用只存在于允许的底层文件。

### 阶段2：编码器常驻采样与逻辑有符号计数

1. 初始化结束后启动 TIM6 和四路编码器并保持运行，不在每段动作之间反复重配 GPIO、启动、停止和清零。
2. 每个动作保存独立的起始计数快照，使用有符号差值计算进度。
3. 为 M1～M4 增加编码器方向修正常量，使车辆逻辑前进时四路计数都为正。
4. 速度环使用有符号增量，不再只用 `abs(delta)`。
5. 增加以下诊断量：
   - 合法正交跳变计数；
   - 非法双位跳变计数；
   - 目标方向与实测方向不一致次数；
   - 每轮最近有效运动时间。
6. 保留四路断线/失速保护；噪声脉冲不能无限延长失速超时。

完成条件：悬空正转时四轮逻辑计数都增加，反转时都减少；错误方向能触发独立故障而不是被绝对值掩盖。

### 阶段3：重构速度执行与低速位置控制

#### 3.1 可连续转动速度区

1. 保留每 20 ms 的速度控制周期作为初始值。
2. 使用每轮前馈 PWM + PI 修正。
3. 加入抗积分饱和：输出达到上限且误差继续同向时暂停积分。
4. 将最小连续 CPS、最小稳定 PWM、启动补偿分别按轮子和方向配置。
5. 启动补偿只作用于确认没有运动的落后轮，避免四轮同时大电流冲击。

#### 3.2 低速、小角度、短距离控制

删除或停用当前依据目标值累加、固定输出 `3100/3300 PWM` 的开环脉冲密度逻辑。改为编码器位置脉冲控制：

1. 每次脉冲前读取四轮剩余计数。
2. 脉冲宽度依据剩余计数、上次脉冲实际增量和每轮响应分别计算。
3. 脉冲后必须等待编码器稳定，再决定下一脉冲。
4. 已领先的轮减少或跳过脉冲，落后轮获得有限补偿。
5. 同侧前后轮应尽量同步，不能让某一轮连续获得脉冲而另一轮长期静止。
6. 设置单脉冲最大时间、最大计数增量和总动作超时。
7. 任何编码器无响应、反向或非法跳变持续异常都立即停车。

不要把这一模式称为“低速 PI”；它是位置反馈控制。

### 阶段4：四轮进度同步与统一到站

对直线和转向动作增加归一化进度：

```text
progress_i = moved_i / target_i
sync_error_i = mean(active progress) - progress_i
target_cps_i = path_target_cps_i + Ksync * sync_error_i
```

要求：

1. 领先轮逐步减速等待，落后轮有限加速。
2. 不再让先到目标的三轮完全停止、只剩最后一轮高功率运动。
3. 四轮均进入位置容差并且速度下降后统一停车。
4. 弧线内外轮目标计数不同，必须比较完成比例而不是直接比较绝对计数。
5. 到站容差、同步增益和最大轮间差应可配置并输出遥测。

### 阶段5：KEY1、KEY2 正常循迹接入编码器内环

重构 `line_tracking.c`：

1. 保留现有四路黑线读取、加权位置、丢线预测和动作状态机。
2. 将函数从“直接执行电机动作”改为“输出底盘命令”，例如：

```c
typedef struct {
  int32_t left_cps;
  int32_t right_cps;
  LineTrackingAction action;
  uint8_t valid;
} LineTrackingCommand;
```

3. 黑线位置误差作为外环，输出线速度与角速度，转换为左右目标 CPS。
4. M1/M2 跟随左侧目标，M3/M4 跟随右侧目标，但每轮由独立编码器环修正。
5. 直线加速使用斜坡，不从低速瞬间跳至最大 CPS。
6. 转弯前降低线速度、提高左右差速；不要仅把 PWM 增益乘到饱和。
7. KEY1 与 KEY2 可以保留不同的上层速度/转向参数，但必须共用同一底层执行器。
8. 丢线 360° 搜索继续使用位置转向控制；重新捕线后用受限速度恢复。

### 阶段6：迁移编码器直线、转向、绕障和轨迹模式

1. `EncoderLinear`、`EncoderStraight`、`EncoderTurn` 只计算四轮目标位置和速度，不直接输出 PWM。
2. 删除 `EncoderTurn` 中绕过底层控制的固定启动冲击和反向 PWM 制动，统一交给 `DriveBase`。
3. KEY1 绕障仍保留：
   - 后退最多 40 mm；
   - 每次 15°；
   - 每次转向后按条件最多前进 40 mm；
   - 侧向红外边界可提前中断；
   - 净转角正负互相抵消；
   - 绝对净转角达到 360°才进入安全故障；
   - 重新找到黑线后进入抑制旧障碍的重挂窗口。
4. KEY3 八字继续使用 220 mm 中心半径和当前几何作为起始值，但内轮使用低速位置控制。
5. 正方形直线保持 400 mm，转角保持90°作为起始目标。
6. 所有轨迹都必须等待四轮同步到站，不能只依据平均计数提前完成。

### 阶段7：电压前馈和停车策略

1. 从 `BatteryMonitor` 读取滤波后的毫伏值，只作为有限前馈补偿，不直接取代速度反馈。
2. 初始建议：

```text
voltage_compensated_pwm = calibrated_pwm * calibration_voltage / measured_voltage
```

3. 限制补偿范围，例如不超过标定值的 `±15%`。
4. 正转和反转分别记录标定参数；不要默认完全对称。
5. 增加两种停车：
   - `COAST`：普通模式切换或低速停止；
   - `BRAKE`：障碍急停或精确轨迹到站时短暂主动制动，然后恢复0/0。
6. 主动制动前确认 AT8236 的 `1/1` 逻辑、最大允许时间和电流；先悬空、再落地低速验证。
7. 低电量显示不等于自动电机欠压保护，除非用户另行授权增加该功能。

### 阶段8：清理维护风险

1. 将旧 `motor.c` 从新工程构建中排除或明确标记为仅供基础实验，防止误调用 `motor_init()` 后把 PWM 引脚改成普通 GPIO。
2. 在 `.ioc` 中正式记录 TIM6 和八个编码器 GPIO；如果必须继续手动初始化，清楚标注 CubeMX 再生成风险。
3. 修正文档和注释不一致：
   - KEY3 当前是一次八字后停车，不是自动无限重复；
   - 重新捕线限制常量与注释必须一致；
   - TIM6 不只服务 KEY3，还服务绕障、正方形和丢线搜索；
   - “黑线闭环”与“编码器轮速闭环”必须明确区分。
4. 保留蜂鸣器仲裁：编码器/传感器安全提示优先于五音节短句。

## 7. 编译与静态检查

每个阶段完成后都要编译整个工程，不能只编译单个文件。检查：

1. 无编译、链接错误和新增警告。
2. 不存在重复 TIM6 中断、重复 EXTI 回调或重复电机输出函数。
3. ELF 中存在新的 `DriveBase_*`、速度控制和四轮遥测符号。
4. 使用 `rg` 检查直接 PWM 写入者是否符合唯一所有权规则。
5. 检查 TIM1、TIM8、TIM2、TIM6 无用途冲突。
6. 生成 ASCII 路径下的 ELF/HEX/BIN/MAP。
7. 输出 HEX 和 BIN 的 SHA-256。

建议产物目录：

`F:\myproject\jidian\project\test-exp7-unified-motion-v1\manual-build-unified-motion`

## 8. 实车分级验收

### A. 电脑端

- 全工程编译、链接成功。
- 静态调用图确认只有唯一电机执行层写 PWM。
- 不得把此阶段描述为实车成功。

### B. 四轮悬空

1. 上电默认停车。
2. 分别给四轮正向、反向低中高目标，确认逻辑计数符号。
3. 记录每轮目标 CPS、实测 CPS、输出 PWM、非法跳变计数和失速状态。
4. 验证单轮阻挡时只补偿该轮并最终安全故障，不出现其他轮持续满功率。
5. 验证数字0或停车按钮能立即取消所有动作。

### C. 落地直线

1. 低速、中速各直行固定距离，测左右偏差。
2. 验证四轮不会在末端变成仅一轮长时间运动。
3. 分别测试正向、反向和不同电量。
4. 调同步增益前先保存原始遥测。

### D. 角度与轨迹

1. 左右15°各重复至少10次，记录平均值、最大误差和是否打滑。
2. 左右90°各重复至少5次。
3. 再测试丢线360°搜索、KEY3八字和数字4正方形。
4. 编码器计数只能证明轮子运动；实际车身角度必须由地面测量确认。

### E. 循迹与绕障

1. 先测 KEY2 纯循迹，再测 KEY1 综合模式。
2. 直线、缓弯、急弯、丢线恢复分别记录目标/实测四轮 CPS。
3. 放置方形障碍，确认红外和超声波安全边界能提前中断动作。
4. 验证绕到另一侧后能重新捕获黑线且旧障碍不会立即再次触发。

## 9. 失败保护与立即停止条件

以下任一情况必须立即停车并报告，不得继续盲目前进：

- 任一目标运动轮超过失速时间没有有效编码器增量；
- 实测编码器方向与命令方向持续相反；
- 非法正交跳变率持续过高；
- 单轮与其余轮进度差超过安全上限；
- 单动作超过动态超时；
- 红外标定/输入失效且处于依赖侧向距离的绕障阶段；
- 超声波近距离确认达到急停阈值；
- 用户停车命令、模式切换或错误处理触发。

## 10. 最终交付内容

接手对话完成后必须给出：

1. 新工程路径和回滚基线路径。
2. 修改、新增和排除构建的文件清单。
3. 当前各模式属于开环、速度闭环、位置闭环还是混合控制。
4. 四轮编码器符号、目标/实测 CPS、同步和故障逻辑说明。
5. 编译输出、警告情况、ELF 符号检查。
6. HEX/BIN/ELF/MAP 路径及 SHA-256。
7. 是否烧录、是否读回验证、是否悬空测试、是否落地测试，分别说明。
8. 尚未实测或仍需用户提供的数据，不得省略。

## 11. 推荐的第一轮实际修改范围

为了降低一次改动过大的风险，第一轮只执行：

1. 创建独立工程和基线清单。
2. 建立唯一 `DriveBase` 输出入口。
3. 让编码器常驻并统一逻辑符号。
4. 保持 KEY1/KEY2/KEY3/数字4的现有目标参数和行为不变。
5. 编译并输出调用所有权检查结果。

第一轮不要同时调整循迹速度、15°转角、八字半径、正方形尺寸、电压补偿和制动参数。确认重构前后行为等价后，再进入低速控制和四轮同步阶段。
