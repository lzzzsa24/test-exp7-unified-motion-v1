# 四轮编码器速度闭环记录

## 实现范围

本文主体保留 2026-09-03 的早期速度环标定记录。当前
`test-exp7-unified-motion-v1` 已由 `DriveBase` 取代旧执行路径：KEY1、KEY2
的黑线外环、KEY3 八字、数字4正方形以及绕障位置段都共用四轮编码器
反馈；`wheel_speed_control.c/.h` 仅作为旧接口兼容层。当前架构与验证边界
以 `README_UNIFIED_MOTION.md` 为准。

控制结构：

1. `motorPWM.c` 的三节点分段标定提供每个电机的前馈 PWM；
2. 每 20 ms 读取 M1～M4 编码器增量；
3. 将逻辑 PWM 映射为目标编码器速度；
4. 每个电机独立使用 PI 修正，修正量限制在 ±500 PWM；
5. 积分限幅，停车或换向时清零，防止重新起步时积分冲击；
6. KEY3 的里程/路径控制只给出目标速度，四轮速度环负责实际执行。

四个编码器现在都参与速度控制、平均里程和失速判断。M2 已改为专项实测
确认的 `PA15/PB3` 完整 AB 正交解码；本统一运动工程不再保留单通道降级。

## 目标速度节点

内部参考经过实测修正，用于抵消启动瞬态和离散 PI 的系统偏差：

| 逻辑 PWM | 内部目标 counts/s | 3秒名义累计目标 |
|---:|---:|---:|
| 2200 | 1412 | 4323 |
| 2400 | 2357 | 7173 |
| 3000 | 5273 | 15954 |

“3秒名义累计目标”沿用开环分段标定后的实际速度刻度；内部参考略低，是
为了让闭环后的实测累计值回到该名义目标。

## 单轮依次运行验证

四轮悬空，每次只运行一个电机，测量窗口 3 s：

| 逻辑 PWM | M1 | M2 | M3 | M4 | 最大目标误差 |
|---:|---:|---:|---:|---:|---:|
| 2200 | 4361 | 4328 | 4307 | 4356 | 0.88% |
| 2400（3轮平均） | 7220.33 | 7192.00 | 7168.67 | 7211.00 | 0.66% |
| 3000 | 16012 | 15978 | 15975 | 15988 | 0.36% |

## 四轮同时运行验证

逻辑 PWM=2400，四轮同时前进，3 s，连续三轮：

| 轮次 | M1 | M2 | M3 | M4 |
|---:|---:|---:|---:|---:|
| 1 | 7182 | 7166 | 7147 | 7198 |
| 2 | 7182 | 7158 | 7151 | 7196 |
| 3 | 7177 | 7162 | 7152 | 7191 |
| 平均 | 7180.33 | 7162.00 | 7150.00 | 7195.00 |

相对累计目标 7173，四轮平均误差分别约为 +0.10%、-0.15%、-0.32%、
+0.31%；共同负载下最快与最慢轮平均差约 0.63%。

## 证据边界

- 编译、自动串口烧录、逐字节回读和四轮悬空测试均已完成。
- 测试验证的是无负载轮速控制，不等于地面轨迹精度；轮胎打滑、配重、
  地面摩擦和转弯侧向阻力仍需地面测试。
- 此文件主体记录的是 2026-09-03 的历史验证状态。统一运动工程中
  KEY1/KEY2 也已接入 DriveBase 四轮速度内环；短距离、小角度和弧线
  低速内轮改用编码器位置脉冲控制。
