# 编码器精确转向功能

## 功能入口

- 遥控数字 `4`：执行一次原地左转 90°。
- 遥控数字 `0`：任何时刻停车。
- USB串口也可发送字符 `4`启动、字符 `0`停车，并持续接收编码器数据。
- 再次测试：先按 `0` 或切换到数字 `1/2/3`，再按 `4`。

通用接口位于 `encoder_turn.c/.h`：

```c
EncoderTurn_Start(angle_mdeg, center_radius_mm, maximum_wheel_cps);
```

- `angle_mdeg`：正数左转、负数右转，例如 `90000`、`-90000`。
- `center_radius_mm`：车体中心轨迹半径；`0` 表示原地旋转。
- `maximum_wheel_cps`：轨迹半径最大车轮的编码器目标速度。

## 几何计算

现有资料没有给出真实轮距；轮径48 mm和每圈1040计数也仍是厂商参数假设。
原始模型曾暂用150 mm估计轮距。

对于车体中心转弯半径 `R` 和转角 `theta`：

```text
left_radius  = R - track_width / 2
right_radius = R + track_width / 2
wheel_counts = abs(theta_mdeg) * abs(radius) * counts_per_rev
               / (180000 * wheel_diameter)
wheel_cps    = max_cps * abs(radius) / max(abs(left_radius), abs(right_radius))
```

半径符号决定车轮方向，因此当 `R=0`、左转为正时，M1/M2 后退，
M3/M4 前进；四个电机分别由速度 PI 控制。

90°原地旋转的纯几何目标约为每轮 812.5 计数。

## 高扭矩低速接近与主动制动

板载驱动的 0/0 停车是滑行，不是主动制动。第一次直接到点停车后，轮子
从约 813 继续滑到 964～1005 计数，对应理论角度约 109.5°。

最初 PWM=2200 的低速脉冲在地面四轮侧向拖曳下动力不足。最终程序采用：

1. 最后 400 计数逐步降速；
2. 低于连续运行阈值时使用高扭矩短脉冲：普通轮 PWM=3300、右前M3
   PWM=3599，每次连续40 ms，平均目标速度1200 counts/s；
3. 起步先执行80 ms高扭矩破静摩擦；
4. 四轮到达公式目标后先停止30 ms，确保H桥安全换向；
5. 施加短反向制动：M1/M2 38 ms、M3 37 ms、M4 42 ms；
6. 再滑行稳定350 ms后判定完成；
7. 编码器失速或总体超时立即停车并报错。

## 最终悬空复测

四轮悬空，`+90°`、`R=0`、`max=1200 counts/s`，连续三轮完全静止后的
编码器计数及理论角度：

| 轮次 | M1 | M2 | M3 | M4 | 编码器推算角度 |
|---:|---:|---:|---:|---:|---:|
| 1 | -798 | +1092 | -815 | -815 | 89.34° |
| 2 | -823 | +1088 | -797 | -815 | 90.22° |
| 3 | -827 | +1100 | -815 | -805 | 90.66° |

平均推算角度 90.07°。M2 使用单通道幅值模式，反向制动时计数仍累加，
所以最终航向估算使用 M1 和 M3/M4；M2 仍参与驱动速度、到点判断和制动。

## 证据边界

这里的角度是根据轮径、轮距和四轮编码器推算的悬空理论角度。悬空时车体
本身没有真实旋转，因此尚未验证地面实际航向。四轮滑移转向在地面会发生
侧滑，最终应以地面量角或陀螺仪结果标定“有效轮距”。

## 地面经验标定

第一次地面90°命令由用户目测实际约40°，因此使用无量纲修正比例
`90/40=2.25`扩大编码器目标。第二次有载荷测试串口正常完成、无故障，
随后用户目测转角差不多为90°。本结果适用于当前地面、轮胎、电池和载荷，
不应把代码中的等效参数理解为真实物理轮距。

继续连续测试三次后，用户观察累计约260°而目标为270°，因此再乘
`270/260=1.0385`，最终无量纲经验比例约为2.337；代码等效参数更新为
351 mm。它仍然不是实物尺寸。

降低M3脉冲功率后又连续测试三次，用户观察每次角度有随机大小变化，累计
约265°而目标为270°。为校正平均值，经验参数按`270/265`微调为约352；
随机侧滑不能由编码器消除。
