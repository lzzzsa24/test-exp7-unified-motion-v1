# 统一运动 V1 四轮悬空测试记录

测试日期：2026-09-04  
串口：COM11，应用串口 115200 8N1  
最终测试固件 BIN SHA-256：
`EE74FA4CBFD885A2F47889C61F88AE0B823DC14225132013DCBE697A80514754`

## 1. 证据边界

本次按用户确认的四轮悬空条件执行。Codex 能控制串口并读取四路编码器、
目标 CPS、实测 CPS、PWM 和故障遥测，但不能通过串口直接看到支架或轮胎
实际朝向。因此下文“运动方向”以用户提供的悬空前提和编码器逻辑方向为
依据；它不是地面车身轨迹、航向或轮胎打滑验证。

## 2. 烧录

最终固件第一次以 115200 写入 62204 字节后，读回到 `0x08005B00` 时收到
异常 ACK `0xF9`，该轮没有被算作验证成功，也没有继续测试。随后以57600
重新完整执行：

```text
BOOTLOADER ACK
ERASE OK: 31 firmware pages; calibration page preserved
WRITE OK: 62204 bytes
VERIFY OK: 62204 bytes
GO OK: 0x08000000
```

启动标识正确：

```text
EXP7 UNIFIED MOTION V1 READY: DEFAULT STOP; 4ENC SIGNED CLOSED LOOP
```

## 3. 测试中发现并修复的问题

1. 首轮 KEY3 测试偶发 M1 故障。原因是低速内轮被同步控制主动暂停时，
   失速/反向检测仍把无输出期间的回弹算成异常。修复后，低速轮只按实际
   脉冲及其完整滑行响应判断断线和反向。
2. 首轮正方形直线通过，但90度转向在目标附近反复越过并以动作超时停止。
   原因是脉冲自适应只统计通电期间计数，漏掉断电后的惯性位移。修复为
   统计“通电＋完整滑行”的总响应，将脉冲范围改为1～20 ms，并要求四轮
   进入容差且速度降下后再统一完成。
3. 运行中停车后，遥测曾保留停止前最后一个实测 CPS。修复为停车状态仍
   每20 ms刷新编码器速度，但保持目标和PWM为0。

每次修改后均重新完成全量编译、静态分析、写入和读回验证。

## 4. 最终固件实测结果

### A. 上电默认停车

- 启动后 `DRV M=0 P=0 F=0`。
- W1～W4 均为 `目标0 / 实测0 / PWM0`。
- 电池测试期间约 8.396～8.519 V，电压采样有效，LOW=0。

### B. 完整 KEY3 八字

- 自动完成时间：约 18.883 s。
- 状态：左圆 `S=1` → 右圆 `S=2` → 完成 `S=4`。
- 最终故障：`F=0`；四路非法跳变 `I=0`。
- 本轮起始全局计数：`2037 / 1993 / 1986 / 2054`。
- 本轮完成全局计数：`21494 / 21441 / 21438 / 21502`。
- 净增量：`19457 / 19448 / 19452 / 19448`；每轮两圆理论目标总和为
  19472。每个单圆的最终 DriveBase 相对计数均进入其位置容差后切换。
- 右圆阶段 M1/M2 为高速外轮，M3/M4 为低速位置脉冲内轮，证明两侧低速
  脉冲路径均被实际覆盖。

### C. 正方形第一边和第一处90度转向

- 第一处转角完成时间（含第一条直线）：约 12.240 s。
- 状态：直线 `S=1` → 转向 `S=3` → 下一边前暂停 `S=4`，`F=0`。
- 400 mm直线相对增量：`2816 / 2806 / 2825 / 2808`，目标2817。
- 90度原地转向相对增量：`-1880 / -1852 / +1871 / +1881`，目标为
  `-1870 / -1870 / +1870 / +1870`。
- 转角完成后的遥测快照存在最多约18计数的残余滑行差；悬空编码器动作
  已完成，但这不能换算为已经验证的地面90度车身转角。

### D. 运行中停车

- 数字4直线运行约850 ms后发送数字0。
- 下一帧为 `DRV M=0 P=0 F=0`，四轮均为 `目标0 / 实测0 / PWM0`，后续
  一帧继续保持全0。
- 测试结束再次发送数字0，当前板上状态为锁存停车。

## 5. 本轮结论与未覆盖项

最终固件已通过以下悬空自动测试：上电默认停车、四轮同向直线、左右两
个八字圆、左侧反转/右侧正转的90度转向、低速内轮互换、完整八字结束、
运行中停车、四路编码器合法采样和无故障完成。

尚未覆盖：逐轮单独低/中/高速、四轮全部反向（本轮只覆盖左侧反转）、
人工阻挡单轮后的补偿与失速停车、高速障碍 BRAKE、KEY1/KEY2 黑线输入、
红外/超声波绕障以及任何有载荷地面轨迹。这些项目不能由本次结果代替。
