# 蜂鸣器短句 40077493715 集成记录

集成日期：2026-09-04  
目标工程：`project/test-exp7-line-bypass-v2`  
回滚副本：`project/backups/test-exp7-line-bypass-v2-before-buzzer-phrase400-20260904`

## 已实现

- PG12 仍为推挽、无上下拉、低速输出，上电默认低电平。
- 新增独立非阻塞模块 `buzzer_phrase_40077493715.c/.h`。
- 固定十步时序总周期为 1530 ms，未使用 PWM、DWT 或阻塞延时。
- 主循环以 1 ms 节拍服务短句任务，并在 OLED、遥测等较长任务之间补充服务点。
- 视觉 `HORN` 事件改为播放一轮五音节短句。
- 原 KEY1、KEY2、KEY3 和数字 4 模式映射不变。
- 新增串口控制：`b` 播放一轮，`B` 播放六轮，`x`/`X` 立即停止。
- PG12 采用显式优先级仲裁：停车/错误、编码器或传感器故障、超声波与绕障安全提示均可立即中断短句；被中断后不会自动续播。
- `ir_avoid_resume_io()` 不再重置或重新配置 PG12，避免红外模块越权覆盖播放状态。

## 编译验证

- 整体工程编译、链接成功，新增模块无编译警告。
- ELF 已包含 `BuzzerPhrase400_Init/Start/Stop/Task/IsPlaying` 五个符号。
- HEX：`manual-build-line-bypass-v2/exp7_line_bypass_v2.hex`
- 当前整体 HEX SHA-256：`69212519C6238EBD23C6D683FD2739676EDA4BC203B4227536E4BE4BDAFD9C3B`
- 本文件首次集成蜂鸣器时的 HEX SHA-256：`80C75935FDDB6A386487F21CAD23A7BB7FF2FC98ABD0950E9565161C13A225AD`

## 实物验收顺序

1. 保持车辆停车或四轮悬空，烧录后确认蜂鸣器上电静音。
2. 串口发送 `b`，应听到一轮、共五个可分辨重音。
3. 串口发送 `B`，相邻两轮起音间隔应约为 1.53 s；发送 `x` 应立即停止。
4. 播放期间触发停车或安全故障，确认安全提示能抢占短句且短句不会自动恢复。
5. 最后再分别检查 KEY1 绕障、KEY2 寻线、KEY3 八字和数字 4 正方形，确认电机行为未受影响。

当前仅完成电脑端编译和静态检查；尚未烧录，也未完成实车听感与安全抢占验证。
