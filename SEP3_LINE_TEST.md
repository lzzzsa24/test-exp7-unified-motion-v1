# 9月3日单向原地搜线测试版

此目录是独立 Git worktree，分支 `test/sep3-predicted-spin`。
基线：当前集成工程 `2540a82`。主工程保持原分支、原文件、原固件。
回退锚点：`rollback/2026-09-05-before-sep3-line-test` -> `2540a82`。

## 定位结论与证据

用户最新反馈：现在四路寻线不如9月3日刚引入原地搜线时；补充确认
“不会后退，沿预测方向转圈”。因此选择**9月3日14:09版**作最接近的候选；
这不能单凭回忆排除同日下午的360度版。

| 北京时间 | 当时实现 |
|---|---|
| 09:43 | 首次优先原地搜索：2200 PWM、550 ms，超时仍后退 |
| 09:47 | 原地搜索增强至2700 PWM，外侧探头增加滚动入弯 |
| 14:09 | 删除后退和反向搜索，只沿预测方向搜索1500 ms；捕线后低速250 ms |
| 14:16 | KEY2新增滤波PD平滑循迹 |
| 14:21 | KEY2新增长直线渐进加速2500至2800 |
| 14:40 | 搜索改为EncoderTurn最多360度、1200 counts/s |
| 14:43 | 搜索速度升至1800 counts/s |
| 9月4日19:12 | 当前仓库首个Git提交3bd3748，已经经过统一运动重构 |

原始记录：
`F:\CodexData\sessions\2026\09\02\rollout-2026-09-02T15-44-26-01a06113-8c3d-7793-96c4-fa2e35750f3b.jsonl`。
该会话跨日；不能按文件名日期判断修改日期。
关键补丁时间为UTC 2026-09-03T06:08:25.403Z，
完成记录为06:09:39.789Z；当时报告36772字节写入及回读通过，
不代表今天的硬件实测。后续补丁为06:15:18、06:15:39、06:20:08、
06:39:08、06:42:10。

恢复参考源文件：
`F:\myproject\jidian\project\backups\test-exp7-line-bypass-v2-far-bold-20260903\Core\Src\line_tracking.c`
SHA256：`734BE84A218B9E040AF8E91451060CB0D0272020BF4BE8D04912DA09ABB0F5DE`。
从这个仍保有早期参数的快照中去掉后加的平滑/加速逻辑，并根据当天补丁
将编码器搜索还原成1500 ms定时搜索。没有找到可确认属于14:09的完整Git提交
或原始HEX，不能称为整车固件逐字节还原。

上午备份也做了交叉核验：名为before_outer_turn_boost的HEX当前哈希为
C78124...，与首次旋转版记录A3451A...不符，不能仅凭备份名字认定版本。
before_fast_corner的HEX则与09:47记录38CB25...相符，但仍包含后退，
所以没有把它选作本次测试固件。

## 此测试版行为与适配边界

- KEY1、KEY2恢复早期离散循迹；基准输入2600，正常居中上限2500。
- 中间两探头离线顺序、最近偏差和最后转向决定搜索方向。
- 丢线先停车70 ms，再单向原地搜索，最多1500 ms；没有方向则停车。
- 搜索请求等效2700 PWM；不后退，也不反向扩大搜索。
- 任意一路重新见线，停止搜索并等待70 ms，再低速恢复250 ms。
- 外侧探头仍按当时逻辑：120 ms滚动入弯、随后原地转向至280 ms，
  然后高曲率前进。恢复期中间两路同时见线仍按预测方向转向。
- 原始PWM请求经过当前DriveBase转换为左右目标CPS，保留四轮速度闭环、
  当前AB编码器接线、电机极性、上电STOP、当前按键/遥控和蜂鸣器。
  因此同样的2700参数不保证与旧版直接PWM具有相同轮速或地面效果。
- 模式切换接口中的smooth/gain设置在此版本为明确的兼容空操作。
- 零基准速度、无效读数或DriveBase故障会清除寻线恢复状态并停车。
- 此版可用来比较早期寻线决策，不能单独验证“旧PWM底层是否更好”。

## 构建与验证

在此worktree根目录运行：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\build_unified_motion.ps1
cmd /c tests\line_recovery\run_sep3.cmd
powershell -NoProfile -ExecutionPolicy Bypass -File .\tools\check_project_state.ps1
git diff --check
```

当前正式构建通过，BIN为61376字节。
BIN SHA256：`F7E8173E5416C9234D887029BA2BDB1D15B166DC4DBE2D25642B83FE74837F88`
HEX SHA256：`85D8B425892776AED7CBF7F4F01D0DD80F3BDA7FE944D98A85C61346C29AE1CA`

主机测试使用MSVC /W4 /WX，通过左右方向、1500 ms边界、15种见线组合、
70/250 ms恢复、无方向停车、无后退、KEY1初次无线路前进、故障、
零速度取消和tick回绕。旧run.cmd和悬空诊断针对新式中间捕线/扩大搜索，
不适用此历史测试，使用run_sep3.cmd与普通正式构建。

未烧录，未做悬空或地面测试。建议烧录后先用KEY2/遥控2比较左右弯道；
遥控0停车。KEY2不包含避障。使用与当前版相同的电量、地面和线路。
需观察是否更容易入弯、找线后是否再次冲出、1.5秒是否不足。
本次没有打开串口或控制小车运动。
